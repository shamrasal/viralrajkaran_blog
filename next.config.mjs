/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Optional: If you want to specify a custom directory for the exported static files
  distDir: 'out',
};

export default nextConfig;
